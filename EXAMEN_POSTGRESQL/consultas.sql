select c.nombre, sum(a.costo_total), p.fecha as ingresos
from alquileres a
join alquileres a on c.
	
select c.nombre, a.id_alquileres
from clientes c
join alquileres a on a.id_alquileres = p.id_alquileres
group by clientes c
limit 5


select p.titulo, a.id_pelicula
from 

select i.id_sucursal, c.nombre
from 